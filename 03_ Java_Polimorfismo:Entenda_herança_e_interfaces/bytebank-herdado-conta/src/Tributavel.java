
// por que interface e n contract 

// tudo dentro de uma interface é abstrato, logo não é necessário escrever abstract
public abstract interface Tributavel {  // Acho que por contrato
	
	double getValorImposto();  // Não preciso escrever public abstract por que é o default
	
}
